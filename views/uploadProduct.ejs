<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Product</title>
    <style>
        * { box-sizing:border-box; margin:0; padding:0; font-family:'Arial', sans-serif; }
        body { background-color:#000; color:#fff; display:flex; justify-content:center; align-items:center; min-height:100vh; padding:20px; }
        .container { background-color:#111; padding:30px; border-radius:12px; width:100%; max-width:500px; box-shadow:0 4px 10px rgba(0,0,0,0.5); }
        h1 { text-align:center; margin-bottom:25px; font-weight:normal; }
        form { display:flex; flex-direction:column; gap:15px; }
        label { font-size:14px; }
        input, textarea { padding:10px; border-radius:8px; border:1px solid #fff; background-color:#000; color:#fff; outline:none; font-size:14px; }
        input::placeholder, textarea::placeholder { color:#aaa; }
        button { padding:12px; border:none; border-radius:8px; background-color:#fff; color:#000; font-size:16px; cursor:pointer; transition:0.2s; }
        button:hover { background-color:#000; color:#fff; border:1px solid #fff; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Upload Product</h1>
        <form action="/products/add-product" method="POST" enctype="multipart/form-data">
            <label for="productName">Product Name:</label>
            <input type="text" id="productName" name="title" placeholder="Enter product name" required>

            <label for="productDescription">Product Description:</label>
            <textarea id="productDescription" name="productDetails" rows="4" placeholder="Enter product description" required></textarea>

            <label for="productPrice">Product Price:</label>
            <input type="number" id="productPrice" name="price" step="0.01" placeholder="Enter price" required>

            <label for="features">Product Features:</label>
            <textarea id="features" name="features" rows="3" placeholder="Enter product features" required></textarea>
            <label for="productImage">Product Image:</label>
            <input type="file" id="productImage" name="productImage" accept="image/*" required>

            <button type="submit">Upload</button>
        </form>
    </div>
</body>
</html>
